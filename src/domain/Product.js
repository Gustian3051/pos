export class Product {
     constructor(id, name, price, stock, supplierId) {
          this.id = id;
          this.name = name;
          this.price = price;
          this.stock = stock;
          this.supplierId = supplierId
     }
}
